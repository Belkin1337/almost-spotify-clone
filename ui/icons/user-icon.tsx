import { UserCheck } from "lucide-react"

const DefaultUserIcon = () => {
	return (
		<>
		</>
	)
}

const UserCheckedIcon = () => {
	return (
		<UserCheck size={18} className="text-neutral-400"/>
	)
}

export { DefaultUserIcon, UserCheckedIcon}